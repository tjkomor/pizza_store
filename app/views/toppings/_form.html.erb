<%= form_with(model: topping, local: true) do |form| %>
  <% if topping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topping.errors.count, "error") %> prohibited this topping from being saved:</h2>

      <ul>
        <% topping.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions" style="padding-top: 10px;">
    <%= form.submit "Create Topping", class: "btn btn-success px-4 py-2" %>
  </div>
<% end %>
