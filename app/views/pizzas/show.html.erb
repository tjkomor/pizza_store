<!-- app/views/pizzas/show.html.erb -->

<h1><%= @pizza.name %></h1>

<h2>Toppings</h2>
<ul>
  <% @pizza.toppings.each do |topping| %>
    <li><%= topping.name %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_pizza_path(@pizza) %> |
<%= link_to 'Back', pizzas_path %>
